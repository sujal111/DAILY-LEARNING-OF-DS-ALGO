
#include <bits/stdc++.h> 
using namespace std; 
void sum_digits(int arr[],int size, int i)
{
    int sum=0;
    if (i==size)
    cout<<endl;
    return 

}
cout << arr[i] << " "; 
sum+=arr[];
	sum_digits(arr, size, i); 
} 
	
#include<bits/stdc++.h>
#include<vector>
using namespace std;
int main(){
    vector<int>v1;
    for(int i=1;i<5;i++)
    v1.push_back();
    cout<<"Output of begin and end is:"
    for(auto i=v1.begin();i!=v1.end();i++)
    cout << *i << " "; 
}